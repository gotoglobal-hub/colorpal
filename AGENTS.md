# AI Agent Instructions

This repository is currently empty. Before making any code or configuration changes, ask the user for the intended language, framework, and application purpose.

## When the repository is empty

- Do not assume a project type, build system, or dependency manager.
- Confirm the target platform (web, desktop, CLI, library, etc.) and preferred stack.
- Recommend creating documentation files such as `README.md`, `CONTRIBUTING.md`, or `ARCHITECTURE.md` once the project scope is defined.

## General guidance for future work

- Keep changes minimal and aligned with the user's stated goals.
- When scaffolding a project, prefer a simple, widely understood structure.
- If the user asks for code, first verify whether they want a sample app, library, or other project type.
- If you need to add customization files later, use `AGENTS.md` or `.github/copilot-instructions.md` and keep content concise.
